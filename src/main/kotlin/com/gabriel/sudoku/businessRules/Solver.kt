package com.gabriel.sudoku.businessRules

class Solver (matrix: Array<Int>) {

    fun solve(matrix: Array<IntArray>): Array<IntArray> {
        val validator = Validator()



        return matrix
    }
}